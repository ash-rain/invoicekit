apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: invoicekit
    component: app
  ports:
    - port: 80
      targetPort: 80
      name: http
